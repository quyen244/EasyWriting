export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>WriteWise</h1>
      <p>
        Scaffold placeholder — the real landing page is built by{" "}
        <code>002-core-app-ux</code>. This project currently ships only the auth
        client/hook (<code>src/lib/auth.ts</code>, <code>src/hooks/useAuth.ts</code>)
        from <code>003-account-authentication</code>.
      </p>
    </main>
  );
}
