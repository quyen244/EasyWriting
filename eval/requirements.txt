# Evaluation workbench dependencies only.
#
# fastapi, uvicorn, sqlalchemy, alembic, psycopg, pyjwt and argon2-cffi were dropped in
# constitution v3.0.0: this package no longer serves HTTP, owns no database, and issues no
# tokens. Supabase provides all three.

pydantic>=2.9
pydantic-settings>=2.5
httpx>=0.27
pyyaml>=6.0
python-dotenv>=1.0

pytest>=8.3
pytest-asyncio>=0.24
