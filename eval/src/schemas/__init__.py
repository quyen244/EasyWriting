"""Scoring schemas.

`account`, `auth`, and `token` were removed in constitution v3.0.0: authentication moved to
Supabase Auth, so this package now covers scoring shapes only. Import from
`src.schemas.assessment` directly.
"""
